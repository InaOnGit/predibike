{
  "name": "predibike",
  "version": "0.0.0",
  "private": true,
  "description": "Application d'aide à la décision budgétaire pour les communes — prédiction du risque accident vélo en Rhône-Alpes (RNCP Développeur IA).",
  "scripts": {
    "release": "semantic-release"
  },
  "devDependencies": {
    "semantic-release": "^24.0.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.0"
  }
}
